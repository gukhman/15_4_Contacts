<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme._15_4_Contacts" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Цвет для тулбара -->
        <item name="colorPrimary">@color/toolbar</item>

        <!-- Применяем кастомный стиль для всех MaterialButtons -->
        <item name="materialButtonStyle">@style/CustomMaterialButtonStyle</item>
    </style>

    <style name="Theme._15_4_Contacts" parent="Base.Theme._15_4_Contacts" />

    <!-- Кастомный стиль для MaterialButton -->
    <style name="CustomMaterialButtonStyle" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/button</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>